# Production-only dependencies for Docker deployment (backend/Dockerfile → Koyeb).
# No heavy ML libs — the flood model is a pure heuristic (numpy only), which
# keeps the image under ~200 MB and fits the free tier.

# Web framework
fastapi>=0.115.2
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.5.0

# HTTP clients (httpx: flood/weather feeds · aiohttp: Twilio SMS · requests: HF TTS)
httpx>=0.27.1
aiohttp>=3.9.1
requests>=2.31.0

# Lightweight ML (numpy only — used for the sigmoid in the flood model)
numpy==1.26.0

# AI Advisory (Groq — fast LLM inference)
groq==0.25.0

# Database — SQLite (aiosqlite) for local dev, Postgres (asyncpg) in prod
sqlalchemy==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0
greenlet>=3.0.0  # required by SQLAlchemy's async engine

# Auth (Supabase JWT verification) & multipart uploads
PyJWT==2.8.0
python-multipart>=0.0.18

# Rate limiting
slowapi==0.1.9
