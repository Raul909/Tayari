# Production-only dependencies for Docker deployment.
# Strips heavy ML libs (LightGBM, scikit-learn, pandas) that the flood
# model doesn't actually require — it uses pure heuristic fallback.
# This keeps the Docker image under 200 MB and fits in Koyeb free tier.

# Web framework
fastapi>=0.115.2
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.5.0

# HTTP client
httpx>=0.27.1

# Lightweight ML (numpy only — used for sigmoid in flood model)
numpy==1.26.0

# AI Advisory (Groq — fast LLM inference)
groq==0.25.0

# SMS
africastalking==1.2.7

# Database — SQLite (aiosqlite) for local dev, Neon/Postgres (asyncpg) in prod
sqlalchemy==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0
greenlet>=3.0.0  # required by SQLAlchemy's async engine

# Scheduling
apscheduler==3.10.4

# Geospatial (lightweight)
geojson==3.1.0

# CORS & multipart uploads
python-multipart>=0.0.18

# Rate limiting
slowapi==0.1.9
