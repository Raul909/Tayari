org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# NOTE: Do not pin org.gradle.java.home here. A machine-specific absolute path
# (e.g. a local macOS JDK) breaks the Ubuntu CI release build and any other
# machine. Gradle resolves the JDK from JAVA_HOME / the Flutter-located JDK
# instead — the app targets Java 21 (see android/app/build.gradle.kts).
